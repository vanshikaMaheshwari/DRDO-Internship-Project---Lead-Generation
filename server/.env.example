PORT=4000

# Cron expression for the automatic re-scrape schedule (default: every 6 hours)
SCRAPE_CRON=0 */6 * * *

# Set to "true" to disable the automatic scheduler (e.g. in tests / CI),
# and only scrape on-demand via `npm run scrape` or POST /api/scrape/run
DISABLE_SCHEDULER=false
